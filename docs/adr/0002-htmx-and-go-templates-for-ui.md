# 0002-htmx-and-go-templates-for-ui

The application will use HTMX and Go `html/template` for the user interface instead of a heavy JavaScript framework. This decision prioritizes simplicity, server-side logic, and performance, while still enabling dynamic features like lazy loading and interactive event sign-ups with minimal custom JavaScript.
